_995‍.x([["calculateTax",()=>calculateTax],["default",()=>calcTaxandSum]]);function calculateTax(price) {
  return Number(price) * 1.2;
}

               function calcTaxandSum(...prices) {
  return prices.reduce((total, p) => (total += calculateTax(p)), 0);
}
